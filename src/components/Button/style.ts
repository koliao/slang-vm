import { createStyles } from '@mantine/core';

export const useStyles = createStyles( () => ( {
	label: {
		display: "flex",
		flexDirection: "row",
		alignItems: "center",
		justifyContent: "center",
	},
	root: {
		display: "flex",
		alignItems: "center",
		justifyContent: "center",
	},
} ) );
